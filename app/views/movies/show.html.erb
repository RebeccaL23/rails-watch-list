<h1><%= @movie.title %></h1>

<%= image_tag @movie.poster_url, alt: @movie.title , width: 200 %>
<p><%= @movie.rating %></p>
<p><%= @movie.overview %></p>

<%# <%= link_to "Back to list", list_path(@list) %>
